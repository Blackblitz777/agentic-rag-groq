# agentic-rag-groq
Rule-based agentic RAG system — routes math queries to a calculator and document queries to TF-IDF retrieval (scikit-learn + cosine similarity) for context extraction from PDFs, with answers generated via Groq Llama 3.1 and persistent chat history. Built from scratch in Python without LangChain.
